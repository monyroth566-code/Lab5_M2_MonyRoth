<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mint_white"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Header Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="20dp">

        <ImageView
            android:id="@+id/imgMoneyIcon"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:src="@drawable/ic_money"
            android:contentDescription="@string/money_icon_desc" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/title_manage_expenses"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="@color/expense_dark"
            android:gravity="center" />

        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="@string/subtitle_welcome"
            android:textSize="15sp"
            android:textColor="@color/expense_green" />
    </LinearLayout>

    <!-- Expense Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardLastExpense"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="30dp"
        app:cardCornerRadius="20dp"
        app:cardElevation="6dp"
        app:cardBackgroundColor="@color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/last_expense_label"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/expense_dark" />

            <TextView
                android:id="@+id/tvLastExpense"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:text="@string/last_expense_initial"
                android:textSize="22sp"
                android:textColor="@color/expense_primary"
                android:textStyle="bold" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Spacer -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="20dp">

        <Button
            android:id="@+id/btnAddExpense"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:text="@string/btn_add_new_expense"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:background="@drawable/btn_primary_bg"
            android:elevation="4dp" />

        <Button
            android:id="@+id/btnViewDetail"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:layout_marginTop="12dp"
            android:text="@string/btn_view_expense_details"
            android:textSize="17sp"
            android:textStyle="bold"
            android:textColor="@color/expense_primary"
            android:background="@drawable/btn_secondary_bg"
            android:elevation="2dp"
            android:enabled="false" />
    </LinearLayout>

</LinearLayout>